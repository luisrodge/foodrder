<div class="row">
  <div class="col-md-12">
    <h2 class="text-title">Restaurants In Santa Elena & San Ignacio</h2><br><br><br>
  </div>
</div>
<div class="row gutter-80">
  <% @restaurants.each do |restaurant| %>
      <div class="col-md-4">
        <%= link_to restaurant_path(restaurant) do %>
            <div class="figure">
              <%= image_tag(restaurant.primary_image.medium, class: "img-responsive") if restaurant.primary_image? %>
              <div class="figcaption">
                <h3><%= restaurant.name %></h3>
                <h4><%= restaurant.location %></h4>
                <div class="figcaption-footer text-muted">
                  <div class="row">
                    <div class="col-md-6">
                      <h4><%= pluralize(restaurant.menus.count, "Menu") %></h4>
                    </div>
                    <div class="col-md-6 text-right">
                      <h4><%= pluralize(restaurant.foods.count, "Total Food") %></h4>
                    </div>
                  </div>
                </div>
              </div>
            </div>
        <% end %>
      </div>
  <% end %>
</div>
