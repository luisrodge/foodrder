<div class="row">
  <div class="col-md-12">
    <h2 class="text-title"><span class="text-bb"><%= pluralize_normal("Food", @foods.count) %> In Santa Elena & San Ignacio</span></h2>
    <br><br><br>
  </div>
</div>
<div class="row gutter-70">
  <% @foods.each do |food| %>
      <div class="col-md-4 col-sm-4 col-xs-6 col-sb">
        <div class="figure">
          <%= image_tag(food.primary_image_url(:medium), class: "img-responsive") %>
          <div class="figcaption">
            <h3 class="truncate"><%= food.name %></h3>
            <h5 class=" text-muted"><%= fa_icon "car" %> Deliverable</h5>
            <h5 >By <%= link_to food.restaurant.name, restaurant_path(food.restaurant) %></h5>
            <div class="figcaption-footer">
              <div class="row">
                <div class="col-md-5">
                  <%= simple_form_for CartItem.new do |f| %>
                      <%= f.input :food_id, as: :hidden, input_html: {value: food.id} %>
                      <%= button_tag(type: 'submit', class: "btn btn-primary btn-sm") do %>
                          <i class="glyphicon glyphicon-shopping-cart"></i> Order
                      <% end %>
                  <% end %>
                </div>
                <div class="col-md-7">
                  <h4 class="text-right text-price"><%= price_format(food.price) %></h4>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
  <% end %>
</div>