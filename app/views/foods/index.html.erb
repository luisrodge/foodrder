<br>
<div class="row">
  <div class="col-md-8">
    <h2 class="text-title remove-margin">
      <% if params[:mc].present? %>
          <span class="text-bb">Feel Like Eating: <%= params[:mc] %></span>
      <% else %>
          <span class="text-bb"><%= pluralize_normal("Food", @foods.count) %> In Santa Elena & San Ignacio</span>
      <% end %>
    </h2>
    <br><br>
  </div>
  <% if params[:mc].present? %>
      <div class="col-md-4 text-right">
        <%= link_to "Show All Foods", foods_path, class: "btn btn-primary btn-gb" %>
      </div>
  <% end %>
</div>

<div class="row">
  <div class="col-md-12">
    <ul class="categories">
      <% Tag.limit(20).each do |tag| %>
          <li><%= link_to tag.name, foods_path(mc: tag.name) %></li>
      <% end %>
    </ul>
  </div>
</div>

<br><br><br>

<div class="row gutter-70">
  <% @foods.each do |food| %>
      <div class="col-md-4 col-sm-4 col-xs-6 col-sb">
        <div class="figure">
          <div class="image-medium-wrapper">
            <%= image_tag(food.primary_image_url(:medium), class: "img-responsive") %>
          </div>
          <div class="figcaption">
            <h3 class="truncate text-strong"><%= food.name %></h3>
            <% if food.restaurant.offers_delivery? %>
                <div class="row">
                  <div class="col-md-6">
                    <h4 class="remove-margin truncate">By <%= link_to food.restaurant.name, restaurant_path(food.restaurant) %></h4>
                  </div>
                  <div class="col-md-6 text-right">
                    <h4 class="remove-margin text-muted"><%= fa_icon "car" %> Deliverable</h4>
                  </div>
                </div>
            <% else %>
                <h4 class="remove-margin truncate">By <%= link_to food.restaurant.name, restaurant_path(food.restaurant) %></h4>
            <% end %>
            <div class="figcaption-footer">
              <div class="row">
                <div class="col-md-5">
                  <%= simple_form_for CartItem.new do |f| %>
                      <%= f.input :food_id, as: :hidden, input_html: {value: food.id} %>
                      <%= button_tag(type: 'submit', class: "btn btn-primary") do %>
                          <i class="glyphicon glyphicon-shopping-cart"></i> Order
                      <% end %>
                  <% end %>
                </div>
                <div class="col-md-7">
                  <h4 class="text-right text-price"><%= price_format(food.price) %></h4>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
  <% end %>
</div>